[package]
name = "parachain-service-bin"
version.workspace = true
edition.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[features]
# Shared test fixtures (`src/mock.rs`) reused across the blob integration tests.
# Off by default so production builds only embed the blob; enabled for this
# crate's own tests via the self dev-dependency below.
test-utils = [
	"dep:codec",
	"dep:executor",
	"dep:jam-node",
	"dep:jam-std-common",
	"dep:jam-types",
	"dep:parachain-authorizer",
	"dep:primitive-types",
	"parachain-authorizer?/test-utils",
	"parachain-service-bin/test-utils",
	"parachain-service/test-utils",
]

[dependencies]
jam-pvm-builder = { workspace = true }

# Optional: only pulled in by `test-utils` for the `mock` fixtures.
executor = { workspace = true, optional = true }
jam-node = { path = "../../vendor/polkajam/crates/node", default-features = false, features = ["hazmat"], optional = true }
jam-std-common = { path = "../../vendor/polkajam/crates/jam-std-common", optional = true }
parachain-authorizer = { workspace = true, optional = true }
codec = { workspace = true, optional = true }
primitive-types = { workspace = true, optional = true }
jam-types = { workspace = true, optional = true }

[build-dependencies]
jam-pvm-builder = { workspace = true }
parachain-service = { workspace = true }
mock-dest-service = { workspace = true }

[dev-dependencies]
parachain-service-bin = { path = ".", features = ["test-utils"] } # TODO clean this up
parachain-authorizer-bin = { workspace = true }
parachain-service = { workspace = true, features = ["test-utils", "std"] }
frameless = { workspace = true }
anyhow = "1" # TODO pull up to workspace
parachain-service-interface = { workspace = true }
