[package]
name = "pvm-builder"
version.workspace = true
edition.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
polkavm-linker = "0.30.0"
jam-program-blob-common = { version = "0.1.28", features = ["std", "polkavm"] }
serde_json = "1.0"
# polkajam's `codec` (the `jam-codec` package) — used for `ConventionalMetadata`'s `Encode`.
# NOT parity-scale-codec.
jam-codec = { workspace = true }
blake2b_simd = { workspace = true }
jobserver = "0.1.32"
