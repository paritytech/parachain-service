[package]
name = "frameless"
version.workspace = true
edition.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
codec = { workspace = true }
tiny-keccak = { workspace = true }
blake2b_simd = { workspace = true }
sp-io = { workspace = true }
parachain-service-core = { workspace = true }

[target.'cfg(target_arch = "riscv64")'.dependencies]
polkavm-derive = { workspace = true }
sp-io = { workspace = true, features = ["disable_allocator", "disable_panic_handler"] }

[features]
default = [
	"std",
]
std = [
	"blake2b_simd/std",
	"codec/std",
	"parachain-service-core/std",
	"sp-io/std",
]

[lints]
workspace = true

# A PVF is entered by name: the service resolves `jam_validate_block` and runs it as a nested
# PVM, so it needs a `generic` blob, the raw program with its export table.
[package.metadata.jam]
type = "generic"
default-cargo-args = ["--no-default-features"]
default-rustflags = ["--cfg", "substrate_runtime"]
